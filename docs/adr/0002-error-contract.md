# ADR-0002：使用稳定错误码和 CLI 退出码

状态：Accepted

## 决策

跨层错误使用 `AppError` 和稳定 `AppErrorCode`。CLI 将错误映射为文档化退出码，中文消息仅用于展示。

## 原因

原实现由各命令捕获任意异常并打印，很多失败仍退出 0，也无法可靠区分会话、网络、协议和文件错误。

## 后果

- 自动化脚本可依赖退出码。
- 远端协议变化不会被误报为空数据。
- 尚未迁移的旧错误由 CLI 兼容分类器暂时处理。
